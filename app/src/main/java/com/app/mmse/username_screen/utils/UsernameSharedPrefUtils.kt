package com.app.mmse.username_screen.utils

//Stores SharedPreferences keys in a central location
class UsernameSharedPrefUtils {
    //Returns the key for username list
    fun getUsernameListKey() = "user2"

    //Returns the key for selected user
    fun getUsernameSelectedKey() = "usernameSelected999999"
}